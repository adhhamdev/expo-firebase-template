# Dependencies
node_modules/
.pnp
.pnp.js

# Expo / Metro
.expo/
dist/
dist-web/
web-build/
expo-env.d.ts
.metro-health-check*

# Generated native projects (CNG / prebuild) — regenerate with `npx expo prebuild`
/android
/ios

# Native / signing secrets (never commit)
*.jks
*.keystore
*.p8
*.p12
*.key
*.mobileprovision
*.pem
credentials.json
credentials/
keystores/
certs/
*.orig.*
.kotlin/

# Env (all local variants)
.env
.env.*
!.env.example

# Firebase native configs (local + any path). Use EAS file env vars for cloud builds.
google-services.json
google-services*.json
GoogleService-Info.plist
GoogleService-Info*.plist
google-services/*.json
google-services/*.plist
!google-services/README.md

# Logs & debug
*.log
npm-debug.*
yarn-debug.*
yarn-error.*
bun-debug.log*

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
*.iml

# TypeScript / test / coverage
*.tsbuildinfo
coverage/
.nyc_output/

# Package managers
# Keep lockfiles committed; ignore only local tool caches
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Local scratch / agent outputs
example
*.plan.md
/dogfood-output
/docs-local
/plan
.turbo/

# Firebase emulators / CLI local state
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
pui-debug.log

# EAS local artifacts
*.apk
*.aab
*.ipa
