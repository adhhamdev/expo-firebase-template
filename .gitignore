# Dependencies
node_modules/
.pnp
.pnp.js

# Expo / Metro
.expo/
dist/
dist-web/
web-build/
expo-env.d.ts
.metro-health-check*

# Generated native projects (CNG / prebuild) — regenerate with `npx expo prebuild`
/android/
/ios/
android/
ios/

# Native / signing secrets (never commit)
*.jks
*.keystore
*.p8
*.p12
*.key
*.mobileprovision
*.pem
credentials.json
credentials/
keystores/
certs/
*.orig.*
.kotlin/

# Env (all local variants)
.env
.env.*
!.env.example

# Firebase native configs at project root (never commit). Use EAS file env for cloud builds.
google-services.json
google-services*.json
GoogleService-Info.plist
GoogleService-Info*.plist

# Logs (all .log files, nested paths, and common tool logs)
*.log
**/*.log
*.log.*
logs/
log/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
bun-debug.log*
lerna-debug.log*
.expo/xdl*.log
metro-*.log
react-native-packager.log
firebase-debug.log
firestore-debug.log
ui-debug.log
pui-debug.log

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
*.iml

# TypeScript / test / coverage
*.tsbuildinfo
coverage/
.nyc_output/

# Package managers
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Local scratch / agent outputs
example
*.plan.md
/dogfood-output
/docs-local
/plan
.turbo/

# Firebase emulators / CLI local state
.firebase/

# EAS local artifacts
*.apk
*.aab
*.ipa
